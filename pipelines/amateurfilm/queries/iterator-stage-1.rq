# iterator.rq
PREFIX sdo: <https://schema.org/>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT $this WHERE {
  $this a sdo:CreativeWork ;
     #uncomment to limit set to 200 resources
	 #sdo:about <http://data.beeldengeluid.nl/gtaa/28142> ;
	 sdo:associatedMedia [rdf:type sdo:VideoObject] .
}
ORDER BY $this