# iterator.rq
PREFIX sdo: <https://schema.org/>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT DISTINCT $this WHERE {
  $this ?p ?o
  FILTER(STRSTARTS(STR($this),"http://data.beeldengeluid.nl/gtaa/"))
}