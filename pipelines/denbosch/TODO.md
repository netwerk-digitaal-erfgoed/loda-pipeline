# TODO note

The transformation of this dataset requires a pre-transform step, as illustrated in schema-ap-nde.rq.

This can be accomplished through [chaining LDE stages](https://ldelements.org/reference/pipeline#chaining-stages), though a configuration mechanism to control this behavior will need to be defined first.